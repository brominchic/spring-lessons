server:
  port: 8080
  servlet:
    context-path: /example-application
spring:
  datasource:
    #   driver-class-name:
    url: jdbc:postgresql://localhost:5555/postgres
    username: postgres
    password: postgres
